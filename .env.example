# Glossary Transfer Configuration
# Copy this file to .env and update the values as needed

# Current environment (dev or prod)
ENVIRONMENT=dev

# Development environment settings
DEV_CREDENTIALS_FILE=auth_files/dom-dx-translation-dev-da60bb26e907.json
DEV_PROJECT_ID=dom-dx-translation-dev
DEV_BUCKET_NAME=dom-dx-translation-dev-bucket

# Production environment settings
PROD_CREDENTIALS_FILE=auth_files/dom-dx-translation-prod-8ae379a2799e.json
PROD_PROJECT_ID=dom-dx-translation-prod
PROD_BUCKET_NAME=dom-dx-translation-prod-bucket

# Supported language pairs (comma-separated)
SUPPORTED_LANGUAGE_PAIRS=en-es,en-fr,en-bs,en-sw

# CSV file settings
CSV_ENCODING=utf-8
CSV_DELIMITER=,
CSV_QUOTECHAR="
CSV_INDEX=False
